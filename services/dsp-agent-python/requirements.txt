# NumPy and SciPy MUST resolve to native ARM64/Accelerate wheels on Apple Silicon.
# Verify: python -c "import numpy; numpy.show_config()" — must show "accelerate".
# Do NOT add --platform or --only-binary flags that would pull manylinux x86 wheels.
numpy>=2.0.0,<3.0.0
scipy>=1.13.0,<2.0.0

# gRPC runtime for the Unix-socket IPC server.
grpcio>=1.64.0,<2.0.0
grpcio-tools>=1.64.0,<2.0.0
protobuf>=5.26.0,<6.0.0

instructor>=1.4.0,<2.0.0
pydantic>=2.7.0,<3.0.0
openai>=1.30.0,<2.0.0   # HTTP client only — talks to Ollama's OpenAI-compatible /v1 endpoint, no OpenAI account/key involved
langfuse>=2.36.0,<3.0.0
psycopg[binary]>=3.1.0,<4.0.0   # If Postgres is choosen over the Langfuse DB (For later)
